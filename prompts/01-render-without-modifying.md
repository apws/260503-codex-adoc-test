# Render-only prompt

```text
Using the existing AsciiDoc toolchain, render the document.
Do not modify source files unless the render fails.
Report:
- command run
- whether node_modules was created
- output path
- errors/warnings if any
```
